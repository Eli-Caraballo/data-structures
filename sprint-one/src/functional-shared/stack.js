var Stack = function() {
  // Hey! Rewrite in the new style. Your code will wind up looking very similar,
  // but try not not reference your old code in writing the new style.

};


var stackMethods = {
  // push: function(value){
  //   this[stackMethods.size()] = value;
  // };,

  // pop: function(){
  //   var val = this[stackMethods.size() - 1];
  //   delete this[stackMethods.size() - 1];
  //   return val;
  // };,

  // size: function(){
  //   return _.size(this);
  // };,

};